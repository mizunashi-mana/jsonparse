var assert = require("chai").assert;

var jsonparse = require("../../build/");
var ConfigParser = jsonparse.ConfigParser;
var ConfigParseError = jsonparse.ConfigParseError;

describe("config parser's base methods test", () => {

});
